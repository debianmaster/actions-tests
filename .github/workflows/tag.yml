name: Build and Deploy in Staging
env:
  GITHUB_CONTEXT: ${{ toJson(github) }}
on:
  push:
    branches: [ master ]
  create:
    tags:
      - 'v*.*.*'
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: set git config
      run: |
        echo $GITHUB_CONTEXT
